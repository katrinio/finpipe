# Storage

Storage отвечает за долговременное хранение данных Finpipe.

Все пользовательские настройки, профили, состояния Telegram и данные интеграций сохраняются в SQLite и переживают перезапуск приложения.

---

# Database Location

По умолчанию база данных хранится в:

text data/finpipe.db 

Каталог data/ создаётся автоматически при запуске приложения.

---

# Data Categories

В системе хранятся два типа данных.

## User Data

Данные, принадлежащие конкретному пользователю:

- профиль работодателя;
- банковские реквизиты;
- настройки Invoice;
- электронная подпись;
- Gmail аккаунт.

---

## System Data

Служебные данные приложения:

- разрешённые пользователи;
- audit log;
- Telegram states;
- OAuth sessions;
- история операций;
- обработанные сообщения;
- обработанные Telegram updates.

---

# Data Lifetime

Следующие данные переживают перезапуск приложения:

- профили пользователей;
- банковские реквизиты;
- Gmail аккаунты;
- подписи;
- настройки Invoice;
- состояния Telegram workflow;
- OAuth sessions;
- audit log;
- история операций;
- список авторизованных пользователей.

---

# Security

Finpipe хранит чувствительные пользовательские данные.

Дополнительно защищаются:

## Signature

Подписи пользователей хранятся в зашифрованном виде.

---

## Gmail OAuth

Refresh token Gmail хранится в зашифрованном виде.

---

## Logging

Чувствительные данные не должны попадать в логи приложения.

В частности:

- IBAN;
- номер счёта;
- email;
- адрес;
- содержимое профиля;
- OAuth токены;
- содержимое документов.

---

# Backup

Для создания резервной копии достаточно сохранить файл базы данных.

Пример:

bash cp data/finpipe.db backup.db 

---

# Recovery

Для восстановления необходимо заменить текущую базу данных резервной копией.

Пример:

bash cp backup.db data/finpipe.db 

После восстановления достаточно перезапустить приложение.

---

# Migration Notes

На текущем этапе проект использует автоматическое создание таблиц через SQLAlchemy.

При запуске приложения отсутствующие таблицы создаются автоматически.

При добавлении новой ORM-модели рекомендуется:

1. Добавить модель в src/storage/orm/.
2. Добавить тесты.
3. Обновить документацию, если изменилась структура хранения данных.

---

# Troubleshooting

## Потеря данных после перезапуска

Проверить:

- используется ли SQLite база данных;
- доступен ли каталог data/;
- не была ли удалена база данных вручную.

---

## Ошибки после обновления приложения

Проверить:

- структура ORM соответствует текущему коду;
- база данных создана актуальной версией приложения.

При необходимости выполнить резервное копирование и пересоздать базу данных.